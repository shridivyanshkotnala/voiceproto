{
  "name": "voiceagent",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo wrapper scripts for deploying the Node server from repository root.",
  "scripts": {
    "postinstall": "npm --prefix server install",
    "start": "npm --prefix server start",
    "dev": "npm --prefix server run dev",
    "build": "echo \"No build step required\""
  },
  "engines": {
    "node": ">=18"
  }
}
