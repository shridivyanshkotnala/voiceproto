export const RETRIEVAL_CONFIG = {
  MIN_SIMILARITY_SCORE: Number(process.env.MIN_SIMILARITY_SCORE || 0.5),
  TOP_K_RESULTS: Number(process.env.RETRIEVAL_TOP_K_RESULTS || 20),
  FINAL_CONTEXT_CHUNKS: Number(process.env.RETRIEVAL_FINAL_CONTEXT_CHUNKS || 12),
}
